# API routes - Must be handled by Cloudflare Workers BEFORE any catch-all redirects
# These routes are handled by Cloudflare Workers Functions
/api/*   200

# Privacy policy page
/privacy    /index.html   200
/privacy-policy    /index.html   200

# Dashboard routing (for main domain)
# All dashboard routes should serve index.html for React Router
/dashboard    /index.html   200
/dashboard/*    /index.html   200

# Approve route for subdomain (stats.lasoireedurire.ca/approve)
# Also available on main domain as /dashboard/approve
/approve    /index.html   200

# Catch-all redirect - must be last to avoid catching API routes
# This ensures all other routes serve the React app
/*    /index.html   200